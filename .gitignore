# Environment
.env
.env.local

# Python
__pycache__/
*.py[cod]
venv/
.venv/

# Node
node_modules/
dist/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Local debug artifacts
latest.json
recent.json
trace_tool.json

# Local reference materials
Ref Screenshots/

# Detached backend server logs
backend/uvicorn.out.log
backend/uvicorn.err.log

# Eval result banks (doc-QA audit)
backend/scripts/results/

# Private notes (separate private repo — see CLAUDE.md Reference Docs)
notes/

# Client source drawings (PDF load schedules) — NEVER publish to this public repo
source-files/
source-drawings/

# Local dev tooling & personal working notes (machine-specific, not part of the project)
.claude/agents/
.claude/commands/gsd/
.claude/get-shit-done/
.claude/hooks/
.claude/gsd-file-manifest.json
.claude/package.json
.mcp.json
gemini/
rag-tool-flow.html

# Playwright test output
frontend/test-results/

# Router table cards — generated by doc-prep, PUBLISHED TO SUPABASE (migration 025).
# Kept on disk as an offline fallback, but never committed: the cards carry sample
# values from a named client building (CCTV level codes, IDF rooms, electrical room
# locations) and this repository is public. Regenerate with:
#   doc-prep/11_table_cards.py --publish
backend/app/data/table_cards.json
